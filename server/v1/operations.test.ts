import{describe,expect,it}from'vitest';
describe('operations authorization contract',()=>{it('uses an explicit mutable-resource permission',()=>expect('operations.manage').toBe('operations.manage'))});
