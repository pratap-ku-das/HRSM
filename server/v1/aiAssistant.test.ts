import{describe,expect,it}from'vitest';import{detectAiIntent}from'./aiAssistant.js';describe('Orbit AI governed intents',()=>{it.each([['How many were absent yesterday?','ABSENT_YESTERDAY'],['Show repeated late attendance this month','REPEATED_LATE'],['What was payroll cost in August?','PAYROLL_COST'],['Which department has highest attrition?','ATTRITION'],['Draft an email about tomorrow holiday','DRAFT_ANNOUNCEMENT'],['Create a leave policy','DRAFT_LEAVE_POLICY']])('%s',(prompt,intent)=>expect(detectAiIntent(prompt)).toBe(intent))});
