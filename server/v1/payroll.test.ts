import express from'express';import request from'supertest';import{describe,expect,it,vi}from'vitest';import{createPayrollRouter}from'./payroll.js';
const appFor=(permissions:string[],prisma:unknown)=>{const app=express();app.use(express.json());const auth=(req:express.Request,_res:express.Response,next:express.NextFunction)=>{Object.assign(req,{auth:{id:'user-a',companyId:'tenant-a',role:'PAYROLL_ADMIN',permissions}});next()};app.use('/api/v1',createPayrollRouter(prisma as never,auth));app.use((error:{code?:string},_req:express.Request,res:express.Response,_next:express.NextFunction)=>res.status(400).json({error:{code:error.code||'VALIDATION_ERROR'}}));return app};
describe('payroll API',()=>{it('denies payroll data without payroll.manage',async()=>{const response=await request(appFor([],{})).get('/api/v1/payroll/config');expect(response.status).toBe(403)});it('rejects a loan for an employee outside the tenant',async()=>{const response=await request(appFor(['payroll.manage'],{employee:{findFirst:vi.fn().mockResolvedValue(null)}})).post('/api/v1/payroll/loans').send({employeeId:'00000000-0000-4000-8000-000000000001',type:'SALARY_ADVANCE',principal:10000,installment:1000,startsOn:'2026-09-01'});expect(response.status).toBe(400);expect(response.body.error.code).toBe('EMPLOYEE_INVALID')});it('requires separate payroll.approve for finance approval',async()=>{const response=await request(appFor(['payroll.manage'],{})).post('/api/v1/payroll/runs/00000000-0000-4000-8000-000000000001/finance-approve');expect(response.status).toBe(403)})});
