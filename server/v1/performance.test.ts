import{describe,expect,it}from'vitest';import{reviewTransition}from'./performance.js';describe('performance review lifecycle',()=>{it('enforces ordered transitions',()=>{expect(reviewTransition('DRAFT','SUBMIT')).toBe('SUBMITTED');expect(reviewTransition('SUBMITTED','MANAGER_REVIEW')).toBe('MANAGER_REVIEWED');expect(reviewTransition('MANAGER_REVIEWED','HR_REVIEW')).toBe('HR_REVIEWED');expect(reviewTransition('HR_REVIEWED','ACKNOWLEDGE')).toBe('ACKNOWLEDGED')});it('rejects skipped stages',()=>expect(()=>reviewTransition('DRAFT','HR_REVIEW')).toThrow())});
